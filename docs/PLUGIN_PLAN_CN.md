# EmbyLyricEnhance 插件阶段开发备忘录

更新日期：2026-08-09  
开发分支：`codex/plugin-settings`  
稳定基线：`b00f0fa`（主题选择器裁剪修复）

## 目标

把当前已经稳定运行的 Emby Web 歌词增强适配器扩展为“Emby C# 插件设置层 + 轻量前端适配器”的混合架构：

- 管理员在 Emby 插件设置页配置服务器默认效果。
- 播放页继续允许当前浏览器即时切换主题。
- 用户本地选择优先于服务器默认主题。
- 未安装、未启动或暂时无法访问 C# 插件时，前端保持现有默认行为，不影响歌词播放。
- 不修改 Emby 通用列表组件，不把歌词样式强制注入原生 Android、iOS 或电视客户端。

## 第一阶段配置范围

- 默认主题：经典累积、单字聚焦、渐变扫光、Apple 风格、简洁整行。
- 字号百分比、行距、字重。
- 高亮色；可选跟随 Emby 主题色。
- 未唱字透明度、辉光强度。
- 当前行缩放、其他行透明度和模糊程度。
- 第二行、第三行及更多子行显示开关。
- 是否允许浏览器本地主题覆盖服务器默认值。

本阶段不做：

- 自动生成翻译、拼音或罗马音。
- 修改原生移动端/电视端歌词页面。
- 在线主题市场、用户账号云同步、复杂权限继承。
- 自动更新或自动替换未知版本的 Emby 前端文件。

## 计划架构

```text
Emby 管理后台
  -> C# PluginConfiguration
  -> 插件配置页保存服务器默认值
  -> 只读配置 API 返回经过约束的公共显示配置

Emby Web 播放页
  -> 现有 lyrics.inject.js
  -> 尝试通过当前 ApiClient 读取插件公共配置
  -> 应用 CSS 自定义属性和服务器默认主题
  -> localStorage 用户主题覆盖（管理员允许时）
  -> 现有解析、60 FPS 插值、原生滚动保持不变
```

## 安全和兼容原则

- 公共配置 API 不返回路径、令牌、用户信息或管理能力。
- 所有数值在 C# 服务端和 JavaScript 客户端双重限制范围。
- 颜色仅接受安全 CSS 颜色格式；无效值回退到 Emby 主题色。
- 前端读取失败、超时、404 或插件缺失时静默使用内置默认值。
- C# 插件 DLL 放入 `/config/plugins` 后需要重启 Emby；前端主题切换仍不需要重启。
- 前端注入继续使用 4.9.5.0 原始文件 SHA-256 和唯一锚点校验。

## 进度清单

### A. 基线与环境

- [x] 从稳定版本建立独立分支 `codex/plugin-settings`
- [x] 确认工作区干净
- [x] 安装并验证 .NET SDK 8.0.423
- [x] 确认 Emby 4.9.5.0 可用的 NuGet/API 依赖版本：`MediaBrowser.Common 4.9.1.90`

### B. C# 插件

- [x] 建立解决方案和插件项目
- [x] 实现 `PluginConfiguration` 默认值
- [x] 实现配置约束和公共 DTO
- [x] 实现 `BasePlugin<TConfiguration>` 入口
- [x] 实现 Emby 管理设置页
- [x] 管理设置页注册左侧服务器菜单，并隔离动态导航产生的旧页面实例
- [x] 用 `data-controller` 加载独立 AMD 控制器，不依赖 Emby 4.9.5.0 会忽略的动态页内联脚本
- [x] 保存配置防重复提交，成功后原地重新读取服务器值
- [x] 实现只读公共配置 API
- [x] 添加配置模型单元测试
- [x] Release 构建通过（0 警告、0 错误）

### C. 前端适配器

- [x] 异步读取插件配置
- [x] 插件缺失/失败安全回退
- [x] 服务器默认主题与浏览器本地覆盖优先级
- [x] CSS 变量驱动字号、行距、字重、颜色、透明度、辉光、缩放和模糊
- [x] 多子行显示开关
- [x] 页面生命周期和旧渲染器隔离保持通过
- [x] 开放结尾逐字歌词回归保持通过

### D. 安装与交付

- [x] 插件构建脚本
- [x] Docker 插件安装/更新脚本
- [x] 将单文件 Release DLL 纳入插件分支，允许 Docker 宿主机直接安装
- [x] 明确 DLL 首次加载需要重启，普通前端切换不需要重启
- [x] 中英文根文档与插件中文安装文档
- [x] JavaScript、C# 核心和 API 契约测试
- [x] `git diff --check`
- [x] 未验证的真实容器检查项单独列出

### E. 部署加固

- [x] 安装前确认 `/config` 持久挂载
- [x] 状态输出包含主 DLL 的 SHA-256，并报告旧版独立 Core DLL 是否残留
- [x] 列出安装备份及是否已经回滚
- [x] 支持恢复最近或指定备份，回滚前另存当前 DLL
- [x] 安装和回滚均支持可选容器重启
- [x] 无参数执行时自动筛选 Emby 候选容器、编号选择并支持手动回退
- [x] 假 Docker 环境覆盖安装、升级、备份、回滚、重复保护和持久化阻断
- [x] 假 Docker 环境覆盖候选选择、非法序号重试、主动手动输入和无匹配回退
- [ ] 在真实 NAS Docker 环境执行同一套验收

### F. 自有音乐播放界面

- [x] 保留 Emby 原生播放内核、会话、投屏和队列状态，不另建音频引擎
- [x] 仅在当前可见歌词页挂载整屏播放器视觉层，页面切换与销毁时移除
- [x] 显示歌曲名、歌手/专辑和可用的主封面
- [x] 显示播放位置、总时长，并把自有进度拖动转发给 Emby 原生 seek 控件
- [x] 转发上一首、播放/暂停、停止、下一首、随机、循环、播放队列和音量操作；静音直接归零并恢复当前播放页音量，避免双重反转
- [x] 四套圆形封面布局支持可记忆的旋转开关
- [x] 同步随机/循环等原生按钮活动状态，并禁用当前不可用操作
- [x] 增加当前会话的注音/第二行开关
- [x] 整屏模式离开歌词页、被其他页面覆盖或销毁时自动撤销页面级样式并恢复原生界面
- [x] 页面状态同步采用幂等 DOM 写入，避免 `body.class` 观察器自触发循环
- [x] 桌面、移动端和 `prefers-reduced-motion` 样式
- [x] 背景、四套布局、五种频谱形态、四种频谱色彩及歌词对齐/字号分别可调并持久化
- [x] 自动化覆盖播放/音量控制转发、静音恢复、进度跳转、注音/旋转开关、布局与频谱持久化、重复实例和页面覆盖
- [ ] 在真实 Emby Web 验证各断点布局、原生控制转发及播放队列打开行为

### G. 后续功能池（按风险逐项推进）

- [ ] 用封面主色生成播放器背景与可读性对比色，失败时保持主题色回退
- [ ] 自有播放队列抽屉；优先复用 Emby 队列数据，不复制播放状态机
- [ ] 播放速度、输出设备/投屏入口
- [ ] 收藏、下载、更多菜单等仅在 Emby 原生能力存在时显示
- [ ] 原文/注音/翻译分别开关，并支持子行角色识别后的独立排版
- [ ] 桌面歌词、歌词偏移与手动校准；偏移必须独立于源歌词文件保存
- [ ] 把播放器面板开关、默认布局和可用功能纳入 C# 管理设置
- [ ] 为不同 Emby 版本建立独立接口锚点和实机兼容矩阵

## 环境检查记录

2026-08-09：

- Windows x64。
- 用户级 .NET SDK 8.0.423，MSBuild 17.11.48。
- Node.js v24.19.0，现有适配器测试可运行。
- 本机没有 Docker CLI，真实 Emby 容器安装验证必须在 NAS/Docker 宿主机完成。
- 用户环境已从 nuget.org 成功恢复 `MediaBrowser.Common 4.9.1.90`。该聚合包自带 `MediaBrowser.Model.dll`；`MediaBrowser.Controller` 和 `MediaBrowser.Model` 不是独立 NuGet 包。
- 当前沙箱仍不能读取用户级 NuGet 配置，提升权限请求又因审批服务 503 未能执行；因此本次真实 `build.ps1` 由用户终端执行。
- 用户随后在真实用户环境执行 `plugin\scripts\build.ps1` 成功：Release 构建 0 警告、0 错误，产物位于 `plugin/artifacts/package/`。

## 2026-08-09 本轮交付记录

- `plugin\scripts\verify.ps1` 通过：C# 配置核心、Emby API 契约桩、`MediaBrowser.Common 4.9.1.90` 真实程序集 API 编译、前端适配器和前后端同步测试均为绿色。
- `node --check adapters\4.9.5.0\lyrics.inject.js`、`git diff --check` 通过。
- Git for Windows 的 `sh -n` 已确认 `plugin/scripts/build.sh` 与 `docker-plugin-install.sh` 语法有效。
- 提交 `80b15a4` 已推送至 `origin/codex/plugin-settings`；GitHub Actions 会进行真实 NuGet 恢复和 Release 构建。
- 部署加固提交 `7d7325d` 已推送至 `origin/codex/plugin-settings`；容器自动发现与交互选择在其后继续开发。
- 0.2.0.0 的设置页嵌入资源已核对；真实 Emby 加载发现双 DLL 依赖绑定失败，0.2.1 改为把配置核心编入主 DLL。
- 0.2.1.0 单 DLL 已使用本地恢复的官方 SDK 完成离线 Release 构建：0 警告、0 错误；产物版本、嵌入设置页及无独立 Core 引用均已核对。
- 0.2.2.0 管理页修复已完成离线 Release 构建：0 警告、0 错误；新增菜单注册、页面去重和保存往返测试。
- 0.2.3.0 针对实机发现的脚本执行早于配置页 DOM 插入时序，改用全局生命周期管理器与 DOM 插入观察器；回归测试覆盖二次进入时删除旧页、绑定新页且不重复注册全局监听；单 DLL 离线 Release 构建为 0 警告、0 错误，版本、依赖和嵌入资源均已核对。
- 0.2.4.0 根据实机截图撤销路由节点删除，改为插件专属隐藏类，避免污染“通用/日志”等页面；设置页透明继承 Emby 主题，并增加读取、保存、服务器回读与错误状态反馈；单 DLL Release 构建为 0 警告、0 错误，嵌入资源已核对。
- 0.2.5.0 实机确认 0.2.4 配置页 HTML 已加载但内联脚本没有执行；现已改用 `data-controller="__plugin/embylyricenhanceconfigjs"` 和嵌入的 `configPage.js`，自动化测试覆盖页面重用、服务器读写往返、双提交和读取失败保护。
- 0.2.6.0 实机确认活动 `lyrics.js` 仍是不读取 DLL 的旧适配器，且设置页 URL 继续命中旧缓存。前端现从 `_connectionmanager` 获取认证 API 客户端；设置页改用 `apiClientResolver` 及新 HTML/JS 资源名；DLL 安装脚本会报告前端配置读取是否已安装。
- 真实容器暴露出旧增强文件存在、但持久原版备份丢失的迁移状态。安装器现使用当前容器的不可变镜像 ID 提取原文件，仅在两个 SHA-256 均匹配时补建备份并重新注入；恢复和重新注入已有独立自动化测试。

## 风险与待确认

- 构建已固定为 `MediaBrowser.Common 4.9.1.90`；升级 SDK 时必须重新执行真实程序集 API 编译和 Emby 容器验证。
- Emby Web 4.9.5.0 歌词模块通过 `_connectionmanager.default.getApiClient(currentItem)` 提供当前认证客户端；接口无会话请求实机返回 401。待 0.2.6 部署后验证有效会话的播放页取值。
- 公共显示接口仅有 GET，实机需要有效 Emby 会话，并只返回清洗后的非敏感视觉字段；管理员修改仍由 Emby 内置插件配置 API 鉴权。
- C# DLL 无法改变原生客户端 UI；这是产品边界，不作为缺陷处理。
- 本机无 Docker，发布前仍需在实际 Emby 4.9.5.0 容器完成 DLL 加载、设置页保存和 Web 配置读取测试。
- 假 Docker 测试能验证脚本状态机和文件恢复，但不能证明 NAS 权限、容器镜像工具集或 Emby 的实际 DLL 加载行为。
- 实机 `/web/ede.user.js:5058` 的 `viewshow` 回调会对空对象写入 `itemId`，并中断 Emby 自身的页面隐藏流程；已观察到 Cinema Intros、CustomCssJS Provider 和歌词页都可被影响。Emby Danmaku Extension 1.47 定向补丁已完成并通过本地测试，待服务器替换后做实机验收。

## 决策日志

- 2026-08-09：保留已验证的单文件 JS/CSS 注入器作为兼容回退；C# 插件先提供设置与配置 API，不在第一步重写歌词核心。
- 2026-08-09：服务器默认配置只影响显示，不接触歌词文件内容和播放控制。
- 2026-08-09：任何插件配置读取错误都不得阻断 `LyricsRenderer`。
- 2026-08-09：只在同一歌词页过渡期间复用一次配置请求；离开并重新进入歌词页后重新读取，使管理员新保存的默认值能够生效。
- 2026-08-09：Docker 插件更新采用时间戳成组备份；单 DLL 安装仍同时记录旧版 Core 文件状态，失败或回滚时恢复原组合。
- 2026-08-09：真实 NuGet 恢复确认只需官方聚合包 `MediaBrowser.Common 4.9.1.90`，同时据真实程序集修正 `IHasWebPages` 和 `BasePluginConfiguration` 契约。
- 2026-08-09：自有播放界面只接管视觉层和用户交互，通过已验证的原生 DOM 控件转发播放操作；Emby 继续唯一持有播放内核、会话、队列和投屏状态。
- 2026-08-09：整屏播放器仅归活动歌词 `LyricsRenderer` 所有，复用现有可见性与重复实例清理规则；页面级类必须由所有者在隐藏、被覆盖或销毁时撤销。
- 2026-08-09：不引用未公开的服务器控制器鉴权特性；公共端点严格保持只读和非敏感字段边界，并以实机 401 行为为准通过当前 Emby 认证客户端读取。
- 2026-08-09：插件部署脚本仅自动推荐运行中的 Emby 候选容器；用户可以选择“没有我要的容器”并手动输入，避免错误猜测容器目标。
- 2026-08-09：Emby 4.9.5.0/.NET 8.0.25 实机确认独立 Core 依赖无法在插件加载上下文中绑定；改为单 DLL，避免依赖解析失败阻断服务器启动。
- 2026-08-09：配置页只绑定动态导航新注入的实例并移除旧副本；保存成功不再调用带导航副作用的通用结果处理器。
- 2026-08-09：配置页脚本不能假设自身页面已在 DOM；管理器驻留在 `window`，监听页面后续插入并始终以 DOM 中最后一个配置页为当前实例。
- 2026-08-09：Emby 管理页 DOM 由其路由器持有，插件不得删除页面节点；重复实例只能以插件私有类暂时隐藏，并在路由生命周期事件中恢复当前实例。
- 2026-08-09：配置页行为使用 Emby 官方的 `data-controller` 路径加载独立 AMD 资源；HTML 只负责结构和安全默认值，不再携带不可靠的内联脚本。
